<% collection_points = CollectionCategory.where("points > #{@collection.total_points}").first.points %>
<% next_collection = CollectionCategory.where("points > #{@collection.total_points}").second %>

<div>
  <h4 style="font-weight: 400">Price: <span><%= humanized_money_with_symbol(@collection.price) %></h4></span>
  <h4 style="font-weight: 400">Number of tracks: <span><%= @collection.tracks.count %></h4></span>
  <h4 style="font-weight: 400">Total points: <span><%= @collection.tracks.count %></h4></span>
</div>



<!--
<ul>
  <% @collection.tracks.each do |track| %>
    <% displayed_track = SingleTrack.find(track) %>
    <li class="display-list"><%= displayed_track.title %></li>
  <% end %>
</ul>
-->